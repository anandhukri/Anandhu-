import { HighlightOnFocusDirectiveDirective } from './highlight-on-focus-directive.directive';

describe('HighlightOnFocusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnFocusDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
